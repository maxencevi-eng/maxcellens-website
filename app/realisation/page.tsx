import ProductionPage, { metadata as productionMetadata } from '../production/page';

export const metadata = productionMetadata;

export default function RealisationPage() {
  return <ProductionPage />;
}
